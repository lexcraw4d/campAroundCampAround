<script>
function handleDelete(e){
    console.log("delete");
    console.log(e.target.id);
}
function handleEdit(e){
    console.log("I clicked pencel edit");
    console.log(e.target.id);
    console.log(e.target.name)
}
</script>
<div class="row">
  <div class="col-md-4 campgrounds-card">
  <% campgrounds.forEach(camp => { %>
    <div class="card " style="width: 30rem">
      <img
        class="card-img-top campground-img"
        style="width: 30rem"
        src="<%= camp.image %>"
        alt="<%= camp.name %>"
        />
        <div class="card-body">
          <h3 class="card-title campground-info"><%= camp.name %></h3> 
          <p class="card-text"><%= camp.description %></p>
          
          <!-- <a href="#" class="btn btn-primary">Go somewhere</a> -->
        </div>
        <div class="btn">

          <i class="fas fa-pencil pencil"><a href="/campgrounds/edit/<%=camp._id%> "></a></i>
        </div>
        <!-- <i id=<%=camp._id %> class="fas fa-trash trash" onClick = handleDelete(event)></i> -->
        <form action="/campgrounds/<%=camp._id%> " method="POST" id="delete-form">
          <input type="hidden" name="_method" value="DELETE">
          <button type="submit" class="btn red">
              <i class="fas fa-trash"></i>
          </button>
      </form>
        
    </div>
    <% }) %>
  </div>
</div>
